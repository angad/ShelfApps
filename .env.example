# Optional local environment. Copy to .env for private values.

# Preferred device identifier used by the local USB installer.
IPHONE_UDID=

# Optional API keys used by companion scripts or app-local data generation.
OPENAI_API_KEY=
NPS_API_KEY=
